{% capture append="oxidBlock_content" %}
    <h1 class="page-header">{{ translate({ ident: "BMNNIT_WIDERRUF_PAGETITLE" }) }}</h1>

    <div class="row">
        <div class="col-12 col-lg-9">
            <p>{{ translate({ ident: "BMNNIT_WIDERRUF_INTRO" }) }}</p>

            {% include "@bmnnit_widerruf/widerruf_form.html.twig" %}
        </div>
    </div>
{% endcapture %}

{% include "layout/page.html.twig" %}
